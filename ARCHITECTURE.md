# منصة الأستاذة الشيماء أحمد — الوثيقة المعمارية

> مخرجات **PHASE 0**. تُحدَّث مع كل مرحلة. آخر تحديث: 2026-09-20.

## 0. الملخص التنفيذي

منصة تعليمية عربية (RTL) بثلاثة أدوار: **طالب · دعم · مدير**.
المرجع المُثبت في الإنتاج: منصة `gehan-saad-lms` (263 طالبًا، 294 كود تفعيل، 2406 سجل تقدّم).
لسنا نبني من فراغ — نبني **إصدارًا أنظف** من معمارية مُجرَّبة، مع سدّ الفجوات التي يفرضها العقد الهندسي.

### ما استُخرج من المنصة المرجعية

| العنصر | القيمة |
|---|---|
| الجداول | 37 جدولًا، RLS مفعّل على كلها |
| دوال RPC | 35 دالة، أغلبها `SECURITY DEFINER` |
| أهم الدوال | `redeem_access_code` · `generate_access_codes` · `start_exam` · `submit_exam` · `log_audit` |
| حرّاس الصلاحية | `is_admin()` · `is_enrolled()` · `can_access_lesson/exam/material/live()` |
| حارس التصعيد | `guard_profile_privileged_columns()` — يمنع الطالب من تعديل `role`/`status` |

### الفجوات التي سنسدّها مقارنةً بالمرجع

1. **دور `support` غير موجود** في المرجع (الأدوار: `student, admin, super_admin`). العقد يفرضه صراحةً كدور لا يملك صلاحيات المدير.
2. **حالات التذاكر ناقصة**: المرجع `open, closed` فقط — العقد يفرض `open, pending, resolved, closed`.
3. **أنواع التذاكر ناقصة**: المرجع نوعان — العقد يفرض أربعة.
4. **حالة الحساب**: المرجع `pending, active, inactive` — العقد يفرض `active, suspended`.
5. **`create_manual_enrollment()` ليست RPC** في المرجع (منطقها في الواجهة) — العقد يفرض تنفيذًا مركزيًا واحدًا (القاعدة 4).
6. **انتهاء الاشتراك** (`expires_at`) غير مُنفَّذ على مستوى القاعدة — العقد يفرض فرضه في الخلفية لا في الواجهة.

---

## 1. حزمة التقنية

تم التحقق من كل إصدار عبر `npm view` بتاريخ 2026-09-20.

| الحزمة | الإصدار | ملاحظة |
|---|---|---|
| next | **16.3.5** | آخر مستقر، App Router |
| react | **19.3.0** | |
| typescript | **5.9.3** | *ليس 7.0.2* — راجع القرار T-1 |
| tailwindcss | **4.3.3** | |
| shadcn | **4.21.0** | مكوّنات Radix، تُنسخ داخل المشروع |
| @supabase/supabase-js | **2.116.0** | |
| @supabase/ssr | **0.12.7** | الجسر الصحيح لـApp Router |
| zod | **4.6.5** | |
| react-hook-form | **7.88.0** | |
| @tanstack/react-query | **5.103.1** | للعميل فقط، لا للخادم |
| lucide-react | **1.47.0** | |
| sonner | **2.0.8** | Toasts |

### قرار T-1 — لماذا TypeScript 5.9 وليس 7.0

`typescript@latest` اليوم هو **7.0.2** (المترجم المكتوب بـGo). لم يُختبر بعد على نطاق واسع مع Next.js 16 وأدوات ESLint/type-checking المصاحبة. العقد يقول "TypeScript strict mode"، لا "أحدث إصدار". نثبّت **5.9.3** لأن الاستقرار مقدَّم على الحداثة في نظام إنتاجي، ونتركها ترقية مؤجَّلة موثّقة.

### قرار T-2 — لماذا لا Supabase CLI محليًا

CLI غير مثبّت على الجهاز، ولا نحتاجه: الهجرات تُطبَّق عبر **Supabase MCP** (`apply_migration`) وتُحفظ نسخة منها في `supabase/migrations/` داخل المستودع لتبقى مُراجَعة ومُتتبَّعة في git.

---

## 2. المعمارية العامة

```
المتصفح
   │  (anon key فقط — لا مفاتيح سرية أبدًا)
   ▼
Next.js App Router
   ├── Server Components ──► lib/queries/*     (قراءة، عبر supabase/server.ts)
   ├── Server Actions ─────► lib/mutations/*   (كتابة + تحقق Zod)
   ├── middleware.ts ──────► تحديث الجلسة + حماية المسارات (UX فقط)
   └── Client Components ──► TanStack Query + Realtime (اشتراكات محدودة)
                                    │
                                    ▼
                          Supabase (PostgreSQL)
                          ├── RLS ◄── طبقة التفويض الحقيقية
                          ├── RPC (SECURITY DEFINER + search_path مُثبَّت)
                          ├── Storage (buckets خاصة + signed URLs)
                          └── Realtime (3 جداول فقط)
```

**القاعدة الحاكمة:** الواجهة لا تُقرّر الصلاحية أبدًا. `middleware.ts` وحرّاس `lib/permissions/` وظيفتهم **تجربة المستخدم** — إخفاء زر، إعادة توجيه. التفويض الحقيقي في RLS وRPC، ويُفترض أن كل طلب قادم من الواجهة عدائي.

### تدفّق التفكير الملزم (البند 51)

```
DATA → AUTHORIZATION → DATABASE → RPC → STATE → UI → UX
```

لن تُبنى شاشة قبل أن يكون جدولها وسياسات RLS الخاصة بها جاهزة ومُختبَرة.

---

## 3. مخطط قاعدة البيانات

### 3.1 الأنواع المعدودة (enums)

```sql
user_role            student | support | admin | super_admin
account_status       active | suspended
course_status        draft | published | archived
enrollment_status    active | expired | cancelled
code_status          active | used | expired | cancelled
question_type        single_choice | multiple_choice | true_false | short_answer
attempt_status       in_progress | submitted | expired
live_status          scheduled | live | ended | cancelled
ticket_type          curriculum_question | subscription_problem | technical_problem | other
ticket_status        open | pending | resolved | closed
```

`super_admin` إضافة فوق العقد: مأخوذة من المرجع، ومبرَّرها أن بعض العمليات (تعيين المديرين، تعديل سجل التدقيق) لا يصح أن يملكها كل مدير.

### 3.2 الجداول

**الهوية والصلاحية**
| الجدول | الغرض | ملاحظات مفتاحية |
|---|---|---|
| `profiles` | ملف المستخدم، مرآة `auth.users` | `role`, `status`, `grade_id`; يُنشأ بـtrigger `handle_new_user` |
| `audit_logs` | سجل الإجراءات الإدارية | `actor_id, action, entity_type, entity_id, metadata, created_at` — إدراج فقط |

**المحتوى**
| الجدول | الغرض |
|---|---|
| `grades` | الصفوف الدراسية (`is_visible`, `sort_order`) |
| `courses` | الكورسات (`grade_id`, `slug`, `status`, `price`) |
| `course_modules` | الوحدات داخل الكورس — *إضافة من المرجع*، الواجهة تعرضها |
| `lessons` | الدروس (`module_id`, `video_url`, `duration_seconds`, `is_free`) |
| `attachments` | المرفقات القابلة لإعادة الاستخدام (`storage_path`, `mime_type`, `file_size`) |
| `course_attachments` | ربط many-to-many بين المرفق والكورسات |

**الاشتراك**
| الجدول | الغرض |
|---|---|
| `enrollments` | وصول الطالب للكورس (`status`, `started_at`, `expires_at`) |
| `activation_codes` | أكواد التفعيل (`code` فريد، `status`, `expires_at`) |
| `activation_redemptions` | سجل الاستخدام — منفصل عن الكود ليبقى أثر تدقيقي |

**التعلّم**
| الجدول | الغرض |
|---|---|
| `lesson_progress` | فريد على `(student_id, lesson_id)`، `progress_percent` مقيَّد 0–100 |
| `exams` · `exam_questions` · `question_options` | بنك الأسئلة |
| `exam_attempts` · `exam_answers` | المحاولات والإجابات، التصحيح في الخادم |
| `live_streams` | الحصص المباشرة والتسجيلات |

**التواصل**
| الجدول | الغرض |
|---|---|
| `notifications` | (`user_id`, `title`, `body`, `link`, `is_read`) |
| `support_tickets` · `support_messages` | التذاكر والمحادثة |
| `push_subscriptions` | *إضافة من المرجع* — الواجهة فيها "تفعيل الإشعارات" |
| `platform_settings` · `static_pages` | *إضافة من المرجع* — شاشة الإعدادات تحتاجهما |

### 3.3 قواعد ملزمة على كل الجداول

- مفاتيح أساسية `uuid` مع `gen_random_uuid()`
- `created_at` / `updated_at` بـ`timestamptz`، والثانية بـtrigger `set_updated_at()`
- `ON DELETE`: `cascade` للأبناء المملوكين (خيارات السؤال)، `restrict` للسجلات المالية (كود مُستخدَم)، `set null` للمراجع الاختيارية
- **لا حذف للسجلات الحرجة** (البند 10): الطالب/الكود/التسجيل تُغيَّر حالتهم ولا تُحذف
- الفهارس تُضاف **بناءً على استعلام حقيقي** موثَّق بجانبها، لا عشوائيًا

### 3.4 الدوال (RPC)

| الدالة | الدور | الحرجية |
|---|---|---|
| `redeem_activation_code(p_code)` | طالب | **عالية** — معاملة ذرّية بـ`FOR UPDATE` على صف الكود |
| `generate_activation_codes(p_course, p_qty, p_expires_at)` | مدير | عالية — توليد عبر `gen_random_bytes` لا تسلسل |
| `create_manual_enrollment(p_student, p_course, p_expires_at)` | مدير | عالية — تنفيذ واحد مركزي (القاعدة 4) |
| `start_exam_attempt(p_exam)` | طالب | عالية — يفرض `max_attempts` ويثبّت `started_at` من الخادم |
| `submit_exam(p_attempt, p_answers)` | طالب | **عالية** — التصحيح كله في الخادم |
| `get_student_progress(p_student)` | طالب/مدير | متوسطة |
| `mark_notification_read(p_id)` | طالب | منخفضة |
| `expire_stale_enrollments()` | مجدولة | متوسطة — تفرض الانتهاء في الخلفية |
| حرّاس: `is_admin()`, `is_support()`, `is_enrolled()`, `can_access_lesson/exam/attachment/live()` | — | تُستخدم داخل سياسات RLS |

كل `SECURITY DEFINER` تُكتب بـ`set search_path = public, pg_temp` صراحةً، ويُتحقَّق من هوية المستدعي في أول سطر.

### 3.5 معاملة تفعيل الكود — نقطة الخطر الأولى

```
authenticate → validate → SELECT ... FOR UPDATE (قفل صف الكود)
   → فحص الوجود/الحالة/الانتهاء → فحص حالة الطالب → فحص التسجيل القائم
   → إنشاء/إعادة تفعيل التسجيل → تسجيل الاستخدام → تعليم الكود مُستخدمًا
   → إنشاء إشعار → COMMIT
```

`FOR UPDATE` هو ما يمنع طلبين متزامنين من استهلاك نفس الكود. أي فشل ⇐ `ROLLBACK` كامل. لا يوجد اشتراك نصف مُفعَّل.

---

## 4. النموذج الأمني

### 4.1 RLS — لا استثناءات

RLS مفعّل على **كل** جدول في `public`. ممنوع `using (true)` على أي جدول حسّاس.

| الدور | يقرأ | يكتب |
|---|---|---|
| طالب | ملفه · تسجيلاته · الكورسات المنشورة · دروس مُصرَّح بها · تقدّمه · محاولاته · إشعاراته · تذاكره | تقدّمه · رسائل تذاكره · محاولاته غير المُسلَّمة |
| دعم | التذاكر المسندة + الحد الأدنى من بيانات الطالب اللازمة للدعم | رسائل التذاكر · حالة التذكرة |
| مدير | كل شيء إداري | كل شيء عدا `audit_logs` (إدراج فقط) |

**ممنوع على الطالب صراحةً:** تغيير `role` أو `status` (يُفرض بـtrigger `guard_profile_privileged_columns` لا بسياسة فقط)، إنشاء أكواد، تعديل تسجيل، لمس بيانات طالب آخر، الوصول لملف خاص بلا تصريح.

### 4.2 التخزين

| Bucket | خاص؟ | شرط الوصول |
|---|---|---|
| `course-images` | لا | عام (أغلفة الكورسات) |
| `attachments` | **نعم** | تسجيل نشط في كورس مرتبط بالمرفق → signed URL قصير العمر |
| `avatars` | لا | صاحب الملف يكتب فقط |
| `support-files` | **نعم** | طرفا التذكرة + المدير |

التحقق من الملف: **MIME الحقيقي + الامتداد + الحجم** — لا يُوثق بالامتداد وحده. أسماء الملفات تُطهَّر قبل التخزين.

### 4.3 أسرار البيئة

```
NEXT_PUBLIC_SUPABASE_URL        ← متصفح ✓
NEXT_PUBLIC_SUPABASE_ANON_KEY   ← متصفح ✓
SUPABASE_SERVICE_ROLE_KEY       ← خادم فقط، لا يُستورد في أي ملف "use client"
CRON_SECRET                     ← خادم فقط
```

`.env.example` يُنشأ بلا قيم حقيقية. أي ملف يستورد مفتاح الخدمة يُمنع من أن يكون مكوّن عميل.

### 4.4 نموذج الأخطاء

أكواد أخطاء ثابتة (`INVALID_CODE`, `CODE_EXPIRED`, `ACCOUNT_SUSPENDED`, `MAX_ATTEMPTS_REACHED` …) تُرفع من RPC وتُترجَم إلى رسائل عربية في طبقة واحدة. **لا تصل رسالة PostgreSQL خام للمستخدم أبدًا.**

---

## 5. بنية المجلدات

```
src/
├── app/
│   ├── (auth)/          login · register · forgot-password · reset-password
│   ├── student/         الرئيسية · كورساتي · تقدمي · الاختبارات · البث · المساعدة · حسابي
│   ├── admin/           نظرة عامة · الطلاب · الصفوف · الكورسات · المرفقات ·
│   │                    أكواد التفعيل · الاختبارات · البث · الإشعارات · التحليلات ·
│   │                    الدعم · الإعدادات
│   ├── support/         التذاكر المسندة فقط
│   └── layout.tsx       <html dir="rtl" lang="ar">
├── components/          ui · layout · auth · student · admin · courses · exams · support · shared
├── lib/
│   ├── supabase/        client.ts · server.ts · middleware.ts
│   ├── queries/         قراءة فقط
│   ├── mutations/       كتابة، كلها عبر Server Actions
│   ├── validations/     مخططات Zod
│   ├── permissions/     حرّاس الواجهة (UX فقط)
│   ├── errors/          الأكواد + الترجمة العربية
│   ├── csv/             تصدير UTF-8 بـBOM
│   └── utils/
├── hooks/
├── types/               database.types.ts (مولَّد من Supabase)
└── middleware.ts
supabase/migrations/     نسخة من كل هجرة، متتبَّعة في git
```

**لا منطق أعمال داخل مكوّنات الواجهة.** المكوّن يستدعي `lib/queries` أو Server Action من `lib/mutations`، ولا يتحدث إلى Supabase مباشرة.

---

## 6. نظام التصميم

مستمدّ من المنصة المرجعية للاتساق البصري بين منصات نفس المالك:

| الرمز | القيمة |
|---|---|
| Primary | أخضر داكن `#2D4A3E` تقريبًا — يُضبط من الصور |
| Surface | أبيض على خلفية `#F7F7F5` |
| Border | رمادي فاتح خفيف جدًا |
| Radius | أزرار ومدخلات أصغر من الكروت (لا radius واحد للكل) |
| الظلال | خفيفة جدًا، على الكروت المرفوعة فقط |
| الخط | عربي احترافي (IBM Plex Sans Arabic أو Tajawal) |
| الأرقام | عربية شرقية في العرض (٢٠٢٦/٩/١٨) |
| الأيقونات | **Lucide** — راجع القرار D-1 |

### قرار D-1 — الأيقونات هنا وليس الصور

المعيار العام المتفق عليه هو «صور بدل الأيقونات». لكن هذا العقد ينصّ صراحةً على `Lucide icons`، وهذا **تطبيق إداري** لا صفحة تسويقية: الأيقونة هنا عنصر وظيفي في جدول وقائمة وزر، والصورة مكانها الصحيح أغلفة الكورسات وصور الدروس. نلتزم بالعقد: Lucide للوظائف، صور حقيقية للمحتوى.

### الاستجابة

الاختبار الفعلي على: **320 · 375 · 390 · 430 · 768 · 1024 · 1280 · 1440+**.
تنقّل الموبايل خمسة عناصر فقط: الرئيسية · كورساتي · تقدمي · الاختبارات · حسابي — والباقي في قائمة ثانوية.

---

## 7. خطة المراحل

| المرحلة | المحتوى | الحالة |
|---|---|---|
| 0 | المعمارية والفحص | ✅ |
| 1 | القاعدة: enums · جداول · فهارس · قيود · دوال · RLS · Storage · Realtime | ✅ |
| 2 | المصادقة والأدوار والمسارات المحمية | — |
| 3 | لوحة المدير الأساسية: إحصائيات · طلاب · صفوف · كورسات · دروس · مرفقات | — |
| 4 | الاشتراكات: أكواد · توليد · CSV · تفعيل · تسجيل يدوي · انتهاء | — |
| 5 | بوابة الطالب: الرئيسية · كورساتي · المشغّل · التقدّم · المرفقات · الإشعارات | — |
| 6 | الاختبارات: الإدارة · الأسئلة · المحاولات · المؤقّت · التصحيح · النتائج | — |
| 7 | البث المباشر والتسجيلات | — |
| 8 | الدعم: التذاكر · المحادثة · Realtime · لوحة الدعم | — |
| 9 | التصليب: تدقيق RLS وStorage وRPC والأداء والاستجابة والوصولية | — |

لا انتقال لمرحلة قبل أن تكون سابقتها متّسقة وعاملة.

---

## 8. مخاطر مفتوحة

| # | الخطر | التعامل |
|---|---|---|
| R-1 | ~~مشروع Supabase غير موجود~~ | ✅ `alshaimaa-ahmed-lms` · `bjhegvkxxtmajyzfampa` · eu-central-1 |
| R-2 | `SECURITY DEFINER` بلا `search_path` = تصعيد صلاحيات | كل دالة تُكتب بـ`search_path` مثبَّت، ويُراجَع في المرحلة 9 |
| R-3 | سباق على نفس كود التفعيل | `SELECT ... FOR UPDATE` + اختبار تزامن فعلي في المرحلة 4 |
| R-4 | مؤقّت الاختبار مُتلاعَب به من العميل | `started_at` من الخادم، والخادم وحده يقرر صلاحية المحاولة |
| R-5 | تسريب ملفات PDF الخاصة | bucket خاص + signed URL قصير + سياسة تتحقق من التسجيل |
| R-6 | Next.js 16 + React 19 حديثان نسبيًا | تثبيت الإصدارات بدقة، وTS 5.9 بدل 7.0 (القرار T-1) |

---

# PHASE 1 — تقرير الإنجاز

**المشروع:** `alshaimaa-ahmed-lms` · `bjhegvkxxtmajyzfampa` · eu-central-1 · نشط
**التاريخ:** 2026-09-20 · **18 هجرة**

## ما نُفِّذ

| المقياس | القيمة |
|---|---|
| الجداول | 25 |
| جداول عليها RLS | **25 / 25** |
| السياسات | 78 |
| سياسات `using(true)` | **0** |
| الأنواع المعدودة | 10 |
| الفهارس | 79 |
| دوال عامة (واجهة API) | 9 |
| دوال داخلية في `private` | 19 |
| `SECURITY DEFINER` بلا `search_path` | **0** |
| حاويات التخزين | 4 (منها 2 خاصة) |
| جداول Realtime | 3 |

## تغييرات القاعدة

الهجرات 01→18: الأنواع · الهوية والتدقيق · المحتوى · الاشتراكات · التعلّم والاختبارات ·
التواصل · فصل روابط الفيديو · حرّاس الصلاحية · RLS · دوال الأعمال · التخزين والـRealtime ·
نقل الداخلي إلى `private` · تصليب أداء RLS · دمج السياسات المتداخلة.

### قرارات بنيوية اتُّخذت أثناء التنفيذ

**D-2 — فصل `video_url` إلى جدول `lesson_videos`.**
RLS تعمل على مستوى الصف لا العمود. إبقاء الرابط داخل `lessons` كان يعني أن أي طالب
يرى فهرس الدروس يقرأ روابط الفيديو المدفوعة. الآن الفهرس مرئي (عنوان/مدة/`has_video`)
والرابط في جدول بسياسة `can_access_lesson`. **اختُبر:** طالب غير مسجَّل ⇐ صفر صفوف.

**D-3 — سكيما `private` لكل ما هو داخلي.**
PostgREST يكشف `public` كاملةً، فكانت 14 دالة داخلية — منها دوال triggers —
متاحة على `/rest/v1/rpc/…` للزوار. نُقلت 19 دالة إلى `private`. السياسات لم تتأثر
لأنها تشير إلى الدوال بـOID لا بالاسم. النتيجة: **37 تحذيرًا ⇐ 9**، والتسعة هي
واجهة الـAPI المقصودة وكلها تتحقق من الهوية في أول أسطرها.

**D-4 — الأسئلة والخيارات محجوبة عن الطالب تمامًا.**
`exam_questions` و`question_options` للإدارة فقط. الطالب يستلمها عبر
`get_exam_attempt_questions` التي تُسقط `is_correct` و`correct_text`.
**اختُبر:** قراءة مباشرة ⇐ صفر صفوف؛ مخرجات الدالة ⇐ لا أثر للإجابات.

**D-5 — `platform_settings` بقائمة بيضاء لا `using(true)`.**
المفاتيح العامة محدَّدة بالاسم، وما عداها للإدارة. يمنع تسريب أي مفتاح يُضاف لاحقًا.

## الاختبارات المُجراة

نُفِّذت بانتحال هوية حقيقية (`request.jwt.claims` + `set role authenticated`) على
أربعة حسابات: مدير · طالب مسجَّل · طالب غير مسجَّل · حساب موقوف.

**الاشتراكات — 8/8**
توليد الأكواد للمدير فقط · تفعيل كود صالح · رفض إعادة الاستخدام · رفض استخدامه من
طالب آخر · رفض التسجيل المكرر · رفض الحساب الموقوف · رفض الكود غير الموجود.

**عزل RLS — 11/11**
طالب لا يرى ملف/تسجيلات/روابط فيديو طالب آخر · لا يعدّد الأكواد · لا يقرأ سجل
التدقيق · لا يرقّي نفسه مديرًا · الموقوف لا يفكّ إيقافه · يعدّل اسمه (يجب أن ينجح) ·
لا يمنح نفسه تسجيلًا.

**الاختبارات — 10/10**
لا قراءة مباشرة للأسئلة أو الخيارات · غير المسجَّل يُرفض · المهلة من الخادم ·
لا تسريب للإجابات · التصحيح في الخادم (50% من 4 درجات) · رفض التسليم المكرر ·
فرض حد المحاولات · رفض تسليم محاولة طالب آخر · لا قراءة لإجابات الغير.

**الكتابة المباشرة — 5/5 (بقياس عدد الصفوف)**
رفع الدرجة · حذف المحاولة · إعادة تفعيل كود مستهلك · تمديد الاشتراك · تلفيق سجل
تدقيق ⇐ **صفر صفوف متأثرة** في كل الحالات.

> **ملاحظة منهجية:** أول صياغة لاختبار "رفع الدرجة" كانت خاطئة — اعتبرت غياب
> الاستثناء نجاحًا للهجوم. RLS تُخفي الصف عن `UPDATE` فلا يتطابق أي صف، وPostgres
> لا يرفع استثناءً لتعديل صفر صفوف. المقياس الصحيح `GET DIAGNOSTICS row_count`.

## اعتبارات أمنية

- الحرّاس في `private` تبقى قابلة للتنفيذ من `authenticated` عمدًا: تعبير سياسة RLS
  يُقيَّم بصلاحية المستخدم المُنفِّذ، وسحب `EXECUTE` يعطّل السياسات كلها. لم تعد
  مكشوفة كـREST لأن PostgREST لا يكشف `private`.
- التسعة تحذيرات المتبقية (`authenticated_security_definer_function_executable`)
  مقبولة بوعي: هي الـAPI نفسه، وكل دالة تتحقق من الهوية والصلاحية قبل أي عمل.
- بيانات الاختبار حُذفت بالكامل: `auth.users = 0` · `profiles = 0` · `courses = 0`.

## الأداء

`auth.uid()` كانت تُقيَّم لكل صف في 16 سياسة — غُلّفت بـ`(select …)` فأصبحت
InitPlan تُقيَّم مرة لكل عبارة. التحذير **اختفى بالكامل (16 ⇐ 0)**.
السياسات التسامحية المتداخلة **21 ⇐ 3**، بفصل سياسات المدير `FOR ALL` عن مسار
القراءة ودمج «صاحب الصف أو المدير» في سياسة واحدة.

## حدود معروفة

| # | الحد | الخطة |
|---|---|---|
| L-1 | ملفات الهجرات ليست في git بعد؛ هي في `supabase_migrations` داخل المشروع | تُسحب بـSupabase CLI عند تهيئة التطبيق في PHASE 2 |
| L-2 | `expire_stale_enrollments()` موجودة لكن غير مجدولة | تُربط بـcron في PHASE 4 |
| L-3 | الإجابة القصيرة تُصحَّح بمطابقة نصية بعد تطبيع عربي؛ لا تتحمّل الأخطاء الإملائية | مراجعة يدوية للمدير في PHASE 6 |
| L-4 | 9 مفاتيح أجنبية (`created_by`/`updated_by`) بلا فهرس **عمدًا** | تُراجَع في PHASE 9 ببيانات استعلام حقيقية |
| L-5 | اتصالات Auth مضبوطة على عدد مطلق (10) لا نسبة | يُضبط قبل الإطلاق |

**المرحلة التالية:** PHASE 2 — تهيئة تطبيق Next.js 16 + المصادقة والأدوار والمسارات المحمية.

---

# PHASE 2 — تقرير الإنجاز

**التاريخ:** 2026-09-20 · هجرتان إضافيتان (19، 20)

## ما نُفِّذ

تهيئة Next.js 16.3.5 + React 19.3 + Tailwind 4.3 + TypeScript 5.9 strict،
ونظام تصميم كامل، ومصادقة عاملة بثلاثة أدوار، وهيكل التطبيق، والشاشة الرئيسية للطالب.

**المصادقة:** دخول · تسجيل · استعادة كلمة المرور · تعيين كلمة مرور جديدة ·
تأكيد البريد · خروج · صفحة الحساب الموقوف.

**الحراسة:** `proxy.ts` يجدّد الجلسة ويوجّه كل دور لمنطقته. الدور يُقرأ من
القاعدة لا من التوكن، لأن بيانات التوكن تتأخّر بعد تغيير الدور.

## تغييرات القاعدة

| الهجرة | المحتوى |
|---|---|
| 19 | `education_stages` · `governorates` · enum `student_section` · أعمدة `profiles`: مرحلة/محافظة/شعبة/مدرسة · RLS للجدولين · فتح القراءة للزوار |
| 20 | بذر مرحلتين وستة صفوف و٢٧ محافظة · تحديث `handle_new_user` ليقرأ الحقول الجديدة |

**لماذا `education_stages` جدول لا نص:** «الصف الأول» يعني شيئًا مختلفًا في
الإعدادي عنه في الثانوي. الفصل يسمح بفلترة الصفوف حسب المرحلة ويمنع اختيارًا متناقضًا.

## اعتبارات أمنية

- **صلاحية الصف تُتحقَّق في الخادم:** `register` يتأكد أن `grade_id` يتبع
  `stage_id` فعلًا قبل إنشاء الحساب. الواجهة تفلتر للعرض، والخادم يفرض.
- **الدور والحالة غير مُمرَّرين في التسجيل إطلاقًا:** القاعدة تضع الافتراضي،
  وtrigger الحارس يمنع تعديلهما بعد ذلك.
- **الحساب الموقوف:** يُسجَّل خروجه فورًا عند محاولة الدخول، ولا تبقى له جلسة.
- **تحويل مفتوح (open redirect):** `next` في الدخول و`next` في مسار التأكيد
  لا يُقبلان إلا إن بدآ بـ`/` ولم يبدآ بـ`//`.
- **استعادة كلمة المرور:** النتيجة واحدة سواء وُجد البريد أم لا، منعًا لتعداد الحسابات.

## الاختبارات المُجراة

في المتصفح على الخادم الحقيقي:

| الحالة | النتيجة |
|---|---|
| كلمة مرور خاطئة | «البريد الإلكتروني أو كلمة المرور غير صحيحة» |
| دخول طالب | ← `/student` · و`/admin`،`/support` يحوّلان إليه |
| دخول مدير | ← `/admin` · يدخل `/support` · و`/student` يحوّله |
| حساب موقوف | رُفض الدخول برسالة واضحة، بلا جلسة |
| خروج | ← `/login` |
| تحقّق التسجيل | خمس رسائل خطأ عربية في وقت واحد |
| المرحلة ← الصف | الإعدادي يعطي ٣ صفوف إعدادية، والثانوي ٣ ثانوية |
| النوع | يظهر للثانوي فقط ويختفي للإعدادي |
| الموبايل 375px | بلا تمرير أفقي · شريط سفلي بخمسة عناصر · الجانبي مخفي |

## ثلاثة أخطاء حقيقية ظهرت وأُصلحت

1. **رسالة خطأ خام للمستخدم.** حقل غائب يصل كـ`null` فترفع Zod رسالتها
   الإنجليزية الافتراضية. أُصلح بتحويل كل حقل إلى نص قبل التحقق، ورسالة
   عربية صريحة لكل قاعدة.
2. **أيقونات Lucide عبر حدود الخادم/العميل.** مكوّن الأيقونة دالة، وReact
   لا يسلسل الدوال. نُقل تعريف التنقّل كاملًا داخل مكوّن العميل، والخادم
   يمرّر اسم المنطقة فقط.
3. **استدعاء دالة من ملف `'use client'` في الخادم.** كل مُصدَّر من ملف عميل
   يصبح مرجعًا للعميل. حُسبت وجهة الشعار محليًا في مكوّن الخادم.

## حدود معروفة

| # | الحد | الخطة |
|---|---|---|
| L-6 | صورة الأستاذة غير متوفرة كملف؛ مكانها `public/brand/teacher.png` | ينسخها المالك |
| L-7 | تأكيد البريد يستخدم قالب Supabase الافتراضي (إنجليزي) | قالب عربي في المرحلة التاسعة |
| L-8 | صفحات الطالب الداخلية (كورساتي/تقدّمي/الاختبارات) لم تُبنَ بعد | المراحل ٥ و٦ |
| L-9 | ثلاثة حسابات اختبار `@test.local` باقية للتطوير | تُحذف قبل الإطلاق |

**المرحلة التالية:** PHASE 3 — لوحة الإدارة: الإحصائيات والطلاب والصفوف والكورسات والدروس والمرفقات.

---

# PHASE 3 + 5 — تقرير الإنجاز

**التاريخ:** 2026-09-20

## سبب هذه المرحلة

بعد PHASE 2 كان الشريط الجانبي يحوي ١٩ رابطًا وصفحة واحدة فقط موجودة.
أي ضغطة على أي رابط آخر تعطي **404**. هذا خرق مباشر للبند 12 («كل ما يبدو
تفاعليًا يجب أن يعمل») وللبند 21 («ممنوع UI وهمي»). بُنيت كل الصفحات.

## ما نُفِّذ

**بوابة الطالب (٧ صفحات):**
الرئيسية · كورساتي (مع تفعيل الكود) · تقدّمي · الاختبارات · البث المباشر ·
المساعدة (تذاكر + محادثة Realtime) · حسابي.

**لوحة الإدارة (١٢ صفحة):**
نظرة عامة · الطلاب (بحث/فلترة/ترقيم/CSV) · الكورسات · أكواد التفعيل (توليد + CSV) ·
الدعم · الصفوف · المرفقات · الاختبارات · البث · الإشعارات · التحليلات · الإعدادات.

**لوحة الدعم:** قائمة التذاكر بصلاحيات الدعم.

## تغييرات القاعدة

لا شيء. كل الصفحات تقرأ من مخطط PHASE 1 وتكتب عبر دواله.

## اعتبارات أمنية

- **تصدير CSV يفحص الدور بنفسه** ولا يعتمد على `proxy.ts`: مسار route يمكن
  استدعاؤه مباشرة بـfetch.
- **حقن الصيغ في Excel:** خلية تبدأ بـ`=` أو `+` أو `-` أو `@` تُسبَق بفاصلة
  عليا قبل التصدير، وإلا نفّذها Excel عند فتح الملف.
- **BOM في أول CSV:** بدونه يفتح Excel على ويندوز الملف بترميز النظام فتظهر
  العربية مشوّشة.
- **بحث الطلاب:** النص يُنظَّف من `,` و`(` و`)` قبل تمريره إلى `or()` —
  وإلا كسر تعبير الفلترة.
- **Realtime محصور في محادثة واحدة** بـ`filter: ticket_id=eq.<id>` لا اشتراك
  عام على الجدول (البند 45).
- **تفعيل الكود بلا أي منطق في الواجهة:** الإجراء يستدعي RPC ويعرض نتيجته فقط.

## الاختبارات المُجراة

| الاختبار | النتيجة |
|---|---|
| ١٩ مسارًا (٧ طالب + ١٢ إدارة) | كلها **200** بعد أن كانت ١٨ منها 404 |
| تفعيل كود صالح (بحروف صغيرة) | نجح · ظهر الكورس · تاريخ الانتهاء ٩٠ يومًا |
| إعادة استخدام نفس الكود | «هذا الكود مُستخدَم من قبل» |
| كود غير موجود | «هذا الكود غير صحيح…» |
| كود فارغ | «اكتب كود التفعيل أولًا» |
| توليد ٣ أكواد من اللوحة | نجح · ظهرت الأكواد قابلة للنسخ |
| تصدير الطلاب والأكواد | CSV برؤوس عربية سليمة |
| جدول الطلاب على 375px | لا تمرير أفقي · الجدول يتحوّل إلى بطاقات |

## خطأ حقيقي ظهر وأُصلح

**ملف `'use server'` لا يصدّر إلا دوال async.** كنت قد صدّرت منه كائنات
التسميات (`TICKET_TYPE_LABELS`) فانهارت صفحة الدعم بـ500.
**الدرس المهم:** `tsc` مرّ بلا خطأ — هذا قيد تشغيلي في Next.js لا قيد أنواع.
اجتياز فحص الأنواع لا يعني أن التطبيق يعمل؛ لا بديل عن تشغيله فعليًا.
الحل: الثوابت في `src/lib/support/labels.ts` والإجراءات وحدها في ملف الخادم.

## حدود معروفة

| # | الحد | الخطة |
|---|---|---|
| L-10 | صفحات الإدارة للعرض؛ الإنشاء والتعديل (كورس/درس/اختبار/بث) لم يُبنَ | المرحلة التالية |
| L-11 | صفحة الدرس `/student/courses/[slug]` ومشغّل الفيديو غير موجودين | المرحلة التالية |
| L-12 | أداء الاختبار (بدء/تسليم) من الواجهة غير مبني — الدوال جاهزة في القاعدة | المرحلة السادسة |
| L-13 | رفع المرفقات إلى Storage من اللوحة غير مبني | المرحلة التالية |

---

# إصلاح حرج — نموذج التسجيل

**التاريخ:** 2026-09-20 · الهجرة 21

## الخطأ الأخطر: قيد الهاتف كان يرفض كل رقم مصري حقيقي

القيد الأصلي `^(\+?20)?1[0125][0-9]{8}$` يقبل `1012345678` و`+201012345678`
ويرفض **`01012345678`** — وهي الصيغة التي يكتبها كل مستخدم مصري، والمكتوبة
حرفيًا في placeholder الحقل نفسه. أي تسجيل حقيقي كان سيفشل.

**لماذا لم يُكتشف مبكرًا:** اختبرت رسائل التحقق (بيانات خاطئة) ولم أُكمل
تسجيلًا ناجحًا واحدًا. الاختبار السلبي وحده لا يكفي — المسار الناجح لا بد أن
يُنفَّذ من أوله لآخره.

**الإصلاح (الهجرة 21):** صيغة واحدة معتمدة `^01[0125][0-9]{8}$` — ١١ رقمًا
بالضبط. البيانات القائمة نُظِّفت قبل تشديد القيد. الطبقات الثلاث متطابقة الآن:
حقل يقبل الأرقام فقط ويقف عند ١١ · مخطط Zod · قيد CHECK في القاعدة.

**اختُبر:** `010/011/012/015` مقبولة · `013` (شبكة غير موجودة) مرفوضة ·
الناقص والزائد والبادئة الدولية والرموز كلها مرفوضة · تسجيل كامل من أوله لآخره
أنشأ الملف بكل الحقول.

## فقدان البيانات عند الخطأ

React يستدعي `form.reset()` بعد كل Server Action — سلوك مقصود منه. النتيجة أن
الطالب يفقد عشرة حقول كلما أخطأ في واحد.

**الإصلاح على مرحلتين:**
1. الحقول أصبحت **متحكَّمًا فيها** من حالة المكوّن، فلا تُفقد عند إعادة الرسم.
2. الحقول النصية عادت، لكن `<select>` ظل يعود بصريًا إلى خيار البداية: قيمته
   في الحالة لم تتغيّر فلم يُعِد React كتابتها في الـDOM بعد الـreset.
   الحل: `key` على النموذج يتغيّر مع كل خطأ فيُعاد تركيبه مُزامَنًا مع الحالة.

**اختُبر:** خطأ في تأكيد كلمة المرور ⇐ الاسم والبريد والهاتف والمرحلة والصف
والمحافظة والشعبة والمدرسة وكلمتا المرور، كلها باقية.

## حقل الهاتف

أرقام فقط: كل حرف أو رمز يُحذف أثناء الكتابة، والطول يقف عند ١١.
اللصق من جهات الاتصال يُنظَّف أيضًا (مسافات، شرطات، `+20`).
عدّاد حيّ تحت الحقل: «أرقام فقط · ٧ من ١١».

**اختُبر:** لصق `010-123 abc 456xyz78901234` ⇒ `01012345678`.

## نقص في ترجمة الأخطاء

`email_address_invalid` من Supabase لم يكن في جدول الترجمة فظهر «خطأ غير
متوقّع». أُضيف مع `validation_failed` و`signup_disabled`.

## ملاحظة تشغيلية

Supabase يحدّ إرسال رسائل التأكيد بشدّة على الخطة الافتراضية (رسائل قليلة في
الساعة). قبل الإطلاق يجب ربط SMTP حقيقي، وإلا فشل تسجيل الطلاب في أوقات الذروة
برسالة «محاولات كثيرة في وقت قصير».

---

# صفحة الكورس ومشغّل الدرس

**التاريخ:** 2026-09-20

## ما نُفِّذ

`/student/courses/[slug]` — فهرس الكورس بالوحدات والدروس، نسبة الإنجاز،
تاريخ انتهاء الاشتراك، والمرفقات.
`/student/courses/[slug]/lessons/[lessonId]` — المشغّل وشرح الدرس والتنقّل
بين الدروس.

## قرارات

**D-6 — الاستحقاق يُستنتج من غياب الرابط لا من فحص في الواجهة.**
`lesson_videos` محكومة بسياسة `can_access_lesson`. إن لم يكن الطالب مستحقًّا
فالاستعلام يعود فارغًا ولا يصل الرابط إلى الخادم أصلًا، فضلًا عن المتصفح.
الواجهة تقرأ هذا الغياب وتعرض رسالة «هذا الدرس للمشتركين» — لا إخفاء بـCSS
ولا رابط مُرسَل ثم مُخفى.

**D-7 — تتبّع التقدّم يختلف باختلاف المشغّل.**
الفيديو المباشر (`<video>`): يُحفظ التقدّم تلقائيًا كل ١٥ ثانية — لا مع كل
حدث `timeupdate` لأنه يُطلق أربع مرات في الثانية. يُستأنف من آخر موضع.
يوتيوب/فيميو: لا يمكن قراءة زمن المشاهدة من إطار طرف ثالث، فالإكمال يدوي بزر،
والنص تحت المشغّل يتغيّر ليشرح الفرق بدل أن يُوهم بتتبّع غير موجود.

**D-8 — الإكمال عند ٩٠٪ لا ١٠٠٪.** آخر الفيديو غالبًا خاتمة وشكر.

**النسبة لا يُوثق بها من المتصفح:** تُقصّ بين ٠ و١٠٠ في الإجراء، وقيد CHECK
في القاعدة يفرض المدى كطبقة أخيرة.

## الاختبارات المُجراة

| الحالة | النتيجة |
|---|---|
| فهرس الكورس | ٣ دروس بمددها، والمجاني موسوم |
| فيديو مباشر | `<video>` + «يُحفظ تقدّمك تلقائيًا» |
| يوتيوب | `<iframe>` + «علّم الدرس كمكتمل بعد مشاهدته» |
| تعليم كمكتمل | نجح · اختفى الزر · حُفظ في القاعدة بنسبة ١٠٠٪ وتاريخ |
| صفحة التقدّم | ١ من ٣ = ٣٣٪ — الحساب من القاعدة لا من الواجهة |
| طالبة غير مشتركة · درس مدفوع | «هذا الدرس للمشتركين» · **لا `<video>` ولا `<iframe>` ولا رابط** |
| طالبة غير مشتركة · درس مجاني | المشغّل يعمل · لافتة «غير مشترك» في رأس الكورس |

## تغييرات أخرى

- التسجيل يتفرّع على وجود الجلسة: تأكيد البريد مقفول ⇐ دخول فوري؛ مفتوح ⇐
  صفحة «راجع بريدك». التفرّع على الجلسة لا على إعداد ثابت.
- فُصلت رسالة حدّ إرسال البريد عن حدّ الطلبات — كانتا تعطيان رسالة واحدة مضلّلة.

---

# إدارة المحتوى — الإنشاء والتعديل

**التاريخ:** 2026-09-20

## ما نُفِّذ

- `/admin/courses/new` — إنشاء كورس.
- `/admin/courses/[id]` — تعديل الكورس، إضافة الوحدات، إضافة الدروس،
  وإظهار/إخفاء كل درس.
- `/admin/live` — جدولة الحصص المباشرة وربط التسجيلات.

## قرارات

**D-9 — معرّف الرابط يُولَّد ولا يُشتقّ من العنوان.**
قيد `courses_slug_format` يقبل الحروف اللاتينية فقط، والعناوين عربية بالكامل،
فلا سبيل لاشتقاقه. يُولَّد `c-xxxxxxxx` من مصدر عشوائي، ويستطيع المدير
استبداله بمعرّف مقروء. بديل النقحرة (transliteration) مرفوض: ينتج معرّفات
بشعة وغير مستقرة، ويتغيّر مع كل تعديل في العنوان فتُكسر الروابط.

**D-10 — الدرس يُخفى ولا يُحذف.**
`lesson_progress` مربوط بـ`ON DELETE CASCADE`، فحذف درس يمحو تاريخ كل طالب
شاهده. الزر «إخفاء/نشر» لا «حذف» (البند 10).

**D-11 — رابط الفيديو يُحفظ في `lesson_videos` لا في `lessons`.**
النموذج حقل واحد، لكن الحفظ يذهب إلى الجدول المحمي: إدخال رابط يُنشئ الصف،
وإفراغ الحقل يحذفه. الفصل هو ما يجعل RLS قادرة على حجب الرابط وحده.

**D-12 — نموذج إضافة الدرس يُفرَّغ بعد النجاح لا بعد الخطأ.**
المدير يضيف دروسًا متتابعة، فإبقاء البيانات بعد النجاح يجعله يحذفها يدويًا
في كل مرة. أما الخطأ فيُبقي كل شيء.

## الاختبارات المُجراة

نُفِّذت من اللوحة كما تفعل الأستاذة:

| الخطوة | النتيجة |
|---|---|
| إنشاء كورس «البلاغة» بسعر ٢٠٠ ومنشور | نجح · تحويل تلقائي لصفحة التحرير · المعرّف `c-6pkvbth7` |
| إضافة وحدة «علم المعاني» | ظهرت في القائمة وفي منسدلة الدروس |
| إضافة درس بفيديو ومدة ووحدة ومجاني | نجح · تحقّقت القاعدة: كل الحقول صحيحة والفيديو في `lesson_videos` |
| إخفاء الدرس | تغيّر فورًا إلى «مخفي» والزر صار «نشر» |
| إعادة النشر | رجع «ظاهر» |
| جدولة حصة مباشرة بـZoom | نجحت · ظهرت مرتبطة بالكورس والموعد صحيح |

## حدود معروفة

| # | الحد | الخطة |
|---|---|---|
| L-14 | إعادة ترتيب الدروس والوحدات بالسحب غير مبنية؛ الترتيب بترتيب الإضافة | لاحقًا |
| L-15 | تعديل درس قائم غير مبني (الإضافة والإظهار/الإخفاء فقط) | التالي |
| L-16 | رفع المرفقات إلى Storage من اللوحة غير مبني | التالي |
| L-17 | إنشاء الاختبارات وأسئلتها من اللوحة غير مبني — دوال القاعدة جاهزة | التالي |

---

# المرفقات — الرفع والتحميل الآمن

**التاريخ:** 2026-09-20

## ما نُفِّذ

رفع من لوحة الإدارة إلى حاوية `attachments` الخاصة، وربط بعدة كورسات،
وتحميل للطالب عبر `/attachments/[id]`.

## النموذج الأمني

**١. نوع الملف يُفحص بالبايتات الأولى.**
الامتداد و`file.type` كلاهما يتحكّم فيه المستخدم ويُزوَّر في سطر واحد.
الفحص يقارن التوقيع الثنائي: `%PDF` للـPDF و`FF D8 FF` لـJPEG و`89 PNG` لـPNG.

**٢. اسم التخزين لا علاقة له باسم الملف الأصلي.**
المسار `YYYY-MM/<uuid>.<ext>`. اسم الملف الأصلي يُحفظ للعرض فقط بعد تنظيفه
من `\ / " \r \n` — محارف تكسر رأس `Content-Disposition` أو تُستخدم لاختراق المسار.

**٣. رابط التخزين لا يصل المتصفح إطلاقًا.**
`/attachments/[id]` يقرأ صف المرفق — وRLS تُرجع null لغير المستحق فالفحص ضمني
وحقيقي — ثم يوقّع رابطًا صالحًا **٦٠ ثانية** ويحوّل إليه. حتى لو سُرّب الرابط
انتهت صلاحيته قبل أن يُنشر.

**٤. لا ملفات يتيمة.** فشل إدراج السجل بعد نجاح الرفع يحذف الملف من التخزين.

## الاختبارات المُجراة

| الاختبار | النتيجة |
|---|---|
| ملف مزوّر: امتداد `.pdf` و`type: application/pdf` ومحتواه `<script>` | **رُفض** — «نوع الملف غير مدعوم» |
| PDF حقيقي باسم عربي مربوط بكورس | نجح · المسار المخزَّن `2026-09/<uuid>.pdf` |
| **مدير** يحمّل | تحويل لرابط موقّع |
| **طالبة غير مشتركة في كورس المرفق** تحمّل | **404 — «هذا الملف غير متاح لك»** |
| نفس الطالبة بعد تفعيل كود الكورس · **نفس الرابط** | تحويل لرابط موقّع ✓ |

السطران الأخيران هما جوهر الاختبار: لم يتغيّر شيء في الواجهة ولا في الرابط،
والقاعدة وحدها هي التي بدّلت النتيجة.

## خطأ متكرّر — والدرس منه

صدّرت `MAX_FILE_BYTES` (رقم) من ملف `'use server'` فسقطت الصفحة كاملة.
**هذه المرة الثانية** لنفس السبب (الأولى كانت `TICKET_TYPE_LABELS`).

**القاعدة:** ملف `'use server'` لا يصدّر إلا **دوال async** وأنواع TypeScript.
أي ثابت يعيش في ملف عادي. أُضيف فحص يمسح `src/lib/mutations/*.ts` بحثًا عن
`export const|let|var|class|enum` — ونتيجته الآن نظيفة.

`tsc` لا يمسك هذا النوع: قيد تشغيلي في Next.js لا قيد أنواع.

## حدود معروفة

| # | الحد | الخطة |
|---|---|---|
| L-18 | حذف مرفق أو فكّ ربطه من اللوحة غير مبني | التالي |
| L-19 | إنشاء الاختبارات وأسئلتها من اللوحة غير مبني | التالي |

---

# الاختبارات — الإنشاء والأداء

**التاريخ:** 2026-09-20

## ما نُفِّذ

**الإدارة:** `/admin/exams/new` و`/admin/exams/[id]` — إعدادات الاختبار،
إضافة أسئلة بأربعة أنواع، عرض الإجابات الصحيحة، ونشر/إخفاء.

**الطالب:** `/student/exams/[id]` — بدء المحاولة، مؤقّت حيّ، إجابة، تسليم،
ونتيجة فورية.

## قرارات

**D-13 — النشر مشروط بوجود سؤال.**
اختبار منشور بلا أسئلة يستهلك محاولة الطالب ويعرض شاشة فارغة. الشرط يُفحص
في الخادم ورسالته تعود منه.

**D-14 — خيارا «صح/خطأ» يُملآن تلقائيًا ولا يُحرَّران.**
كتابتهما يدويًا مصدر أخطاء إملائية بلا أي فائدة.

**D-15 — المؤقّت يُحسب من فارق ساعة الخادم.**
`server_now` يعود مع المحاولة، ويُقاس الفارق عن ساعة الجهاز مرة واحدة
ويُطبَّق على كل نبضة. تغيير ساعة الجهاز لا يمنح ثانية إضافية، والخادم يرفض
التسليم المتأخر على أي حال (البند 20).

**D-16 — انتهاء الوقت يُسلّم تلقائيًا.**
ترك الطالب أمام شاشة ميتة يضيّع إجاباته التي كتبها فعلًا.

**D-17 — `z.guid()` لا `z.uuid()`.** انظر القسم التالي.

## خطأ حقيقي: تعارض بين طبقتَي التحقق

`z.uuid()` في Zod 4 يتحقّق من **نسخة** المعرّف طبقًا لـRFC 4122 (الخانة 13
من 1–8، والخانة 17 من 8/9/a/b)، بينما نوع `uuid` في PostgreSQL يقبل أي ٣٢
خانة سداسية بالتنسيق المعروف.

النتيجة: معرّف مقبول في القاعدة **ومرفوض في التطبيق**. ظهر حين رفض النموذج
كورسًا موجودًا فعلًا لأن معرّفه لم يكن من نسخة معيارية.

الخطورة أبعد من بيانات الاختبار: أي معرّف قادم من نظام آخر، أو مُولَّد بأداة
لا تلتزم بالنسخة، يصبح غير قابل للاستخدام رغم قبوله في القاعدة.

**المبدأ:** طبقتا التحقق يجب أن تتفقا. الأشدّ ليست الأصحّ إن رفضت ما تقبله
القاعدة. أُنشئ `src/lib/validations/shared.ts` بـ`z.guid()` (تنسيق بلا نسخة)
وحلّ محل كل `.uuid()` في المشروع.

## الاختبارات المُجراة

بُني اختبار كامل من اللوحة ثم أُدّي كطالبة:

| الخطوة | النتيجة |
|---|---|
| إنشاء اختبار: مدة دقيقتان · نجاح ٥٠٪ · ٣ محاولات | نجح |
| سؤال اختيار واحد (٢ درجة) | نجح |
| سؤال صح/خطأ (١ درجة) | نجح · الخياران مُملآن تلقائيًا |
| سؤال إجابة قصيرة (٢ درجة) | نجح |
| النشر | نجح بعد وجود الأسئلة |
| **الطالبة: فحص تسريب الإجابات في HTML الصفحة** | **`is_correct` و`correct_text` ونص الإجابة: غير موجودة إطلاقًا** |
| المؤقّت | ظهر `١:٤٦` ويعدّ تنازليًا |
| الإجابة: سؤالان صح وواحد خطأ عمدًا | عدّاد «أجبت ٣ من ٣» |
| التسليم | **٣ من ٥ = ٦٠٪ · ناجح** |
| تحقّق من القاعدة | `2.00` + `1.00` + `0.00` — التصحيح من الخادم سؤالًا سؤالًا |

السطر قبل الأخير هو جوهر البند 19: الدرجة لم تُحسب في المتصفح، والمتصفح لم
يرَ إجابة صحيحة واحدة قبل التسليم.

## حدود معروفة

| # | الحد | الخطة |
|---|---|---|
| L-20 | مراجعة إجابات المحاولة بعد التسليم غير مبنية للطالب | لاحقًا |
| L-21 | تعديل سؤال أو حذفه بعد إضافته غير مبني | لاحقًا |
| L-22 | الإجابة القصيرة تُصحَّح بمطابقة نصية؛ «جملة اسمية» تُرفض ولو كانت مقبولة تربويًا | مراجعة يدوية للمدير |

---

# الصفحة الرئيسية العامة

**التاريخ:** 2026-09-20

## لماذا لم تكن موجودة

كان `/` مجرد `redirect('/login')`. النتيجة أن الزائر لا يجد شيئًا يشرح المنصة
قبل أن يُطلب منه حساب — وهذا يخالف البند 26: كل قسم له هدف، والصفحة الأولى
هدفها الإقناع لا الحجب.

## ما نُفِّذ

`/` صفحة عامة للزائر: واجهة بوعد واحد، ما يجده في حسابه، الصفوف المغطّاة،
ثلاث خطوات للبدء، ودعوة واحدة للتسجيل.
المستخدم المسجَّل لا يراها: `proxy.ts` يحوّله إلى منطقته.

## قرارات

**D-18 — الصفوف تُقرأ من القاعدة لا تُكتب في الصفحة.**
لو كُتبت يدويًا، فأي صف يُضاف من اللوحة لا يظهر للزائر، وأي صف يُخفى يبقى
معروضًا. سياسات `anon` على `education_stages` و`grades` تتيح القراءة للزائر.

**D-19 — عدد الكورسات يظهر فقط إن كان أكبر من صفر.**
«٠ كورس متاح» رسالة تطرد الزائر لا تجذبه.

**D-20 — الصورة تختفي على الموبايل.**
الأولوية هناك للنص والزر، لا لصورة تدفع الدعوة إلى أسفل الشاشة.

**النسخ الثابت:** `revalidate = 3600` — المحتوى مرجعي لا شخصي، فلا داعٍ
لاستعلام القاعدة مع كل زيارة.

## الاختبارات المُجراة

| الاختبار | النتيجة |
|---|---|
| زائر بلا جلسة | الصفحة تظهر كاملة |
| الصفوف المعروضة | ٦ صفوف من القاعدة (٣ إعدادي + ٣ ثانوي) |
| عدد الكورسات | «٢ كورس متاح الآن» — رقم حقيقي |
| مستخدم مسجَّل يفتح `/` | يُحوَّل إلى منطقته |
| الموبايل 375px | لا تمرير أفقي · الصورة مخفية · الدعوة ظاهرة فوق الطيّة |

---

# مراجعة المحاولة · الأنميشن · جودة الصورة والنص

**التاريخ:** 2026-09-20 · الهجرة 22

## مراجعة إجابات المحاولة

`/student/exams/[id]/attempts/[attemptId]` — كل سؤال بدرجته، وما اختاره
الطالب، والإجابة الصحيحة، وشرحها إن وُجد.

**الدالة `get_exam_attempt_review`** هي اللحظة الوحيدة التي تُكشف فيها
الإجابات الصحيحة للطالب. الشرط `status = 'submitted'` بداخلها هو ما يمنع
استدعاءها أثناء المحاولة لسرقة الإجابات.

**اختُبر على مستوى القاعدة قبل بناء الواجهة:**

| الحالة | النتيجة |
|---|---|
| صاحب المحاولة بعد التسليم | ٦٠٪ · ٣ أسئلة |
| هل تظهر الإجابات الصحيحة؟ | نعم — وهذا هو المقصود بعد التسليم |
| طالبة أخرى تراجع محاولة غيرها | **FORBIDDEN** |
| **مراجعة محاولة ما زالت جارية** | **ATTEMPT_NOT_SUBMITTED** |

الحالة الأخيرة كانت الباب الخلفي المحتمل: لولا الشرط لأمكن فتح المراجعة في
تبويب ثانٍ أثناء الاختبار وقراءة كل الإجابات.

## أنميشن الظهور من الأسفل

أداة `rise-in` في نظام التصميم لا حركة مكتوبة في مكان واحد: ٠٫٥٥ ثانية،
مسافة ١٫٢٥rem، و`--rise-delay` للتتابع. `both` يبدأ العنصر مخفيًا فلا يومض.
قاعدة `prefers-reduced-motion` القائمة تُلغيها لمن طلب تقليل الحركة (البند 14).

طُبِّقت على لوحة الهوية في صفحات المصادقة (اسم ← صورة ← نص، بتتابع
0/120/260ms) وعلى واجهة الصفحة العامة.

## خطأ حقيقي: الصورة كانت تُمدَّد

`sizes="32rem"` كان **كذبًا على Next**: أخبرته أن الصورة لن تتجاوز 512px،
بينما اللوحة تأخذ ما تبقّى بعد عمود النموذج = **768px**. فقدّم 640px عند
جودة 75، والمتصفح مدّدها.

**القياس قبل وبعد:**

| | قبل | بعد |
|---|---|---|
| العرض المعروض | 768px | 768px |
| العرض المُقدَّم | **640px — تمديد** | **828px** |
| الجودة | 75 | 90 |

`sizes` الصحيح: `(min-width: 1024px) calc(100vw - 32rem), 0px`.
و`images.qualities` في `next.config.ts` لأن Next 16 يرفض أي قيمة غير مُعلَنة.

**الدرس:** `sizes` ليس تزيينًا — هو العقد الذي يبني Next عليه `srcSet`.
قيمة خاطئة فيه تُنتج صورة مشوّشة مهما رُفعت الجودة.

## جودة النص العربي

- `font-kerning` و`font-variant-ligatures`: العربية تعتمد على الوصلات،
  وإيقافها يكسر شكل الكلمة — ليس تحسينًا اختياريًا.
- `text-wrap: balance` على العناوين: تمنع سقوط كلمة وحيدة في سطر أخير،
  وهو أوضح في العربية لطول كلماتها.
- `text-wrap: pretty` على الفقرات.
- `tabular-nums` على العدّادات والجداول: الأرقام تتراصّ فلا يرقص السطر مع
  كل ثانية في مؤقّت الاختبار.

## خطأ في طريقة عملي

استبدلتُ نصًّا في ملف بـ`replace` دون التحقق من أنه طابق، فمرّ التعديل
صامتًا ولم يظهر رابط المراجعة. اكتُشف بالاختبار في المتصفح لا بالفحص.
**القاعدة:** كل استبدال نصّي يُتبَع بتأكيد أنه طابق فعلًا.

---

# المرحلة ٩ — إتمام التحرير: الأسئلة والدروس والمرفقات

## ما نُفِّذ
| الفجوة | ما صار متاحًا |
|---|---|
| L-21 | تعديل السؤال وحذفه وإعادة ترتيبه من صفحة الاختبار، مع حقل «شرح الإجابة» |
| L-15 | تعديل درس قائم في مكانه: العنوان، الفيديو، المدة، الوحدة، الشرح، المجانية، النشر |
| L-14 | إعادة ترتيب الأسئلة والدروس بأزرار تقديم/تأخير |
| L-18 | حذف مرفق، وفكّ ربطه عن كورس، **وربطه بكورس آخر** |

## قرار D-21 — الاختبار يُقفَل ببدء أول محاولة
محاولة واحدة تحوّل الاختبار من مسوّدة إلى سجلّ. بعدها:

| العملية | قبل أول محاولة | بعدها |
|---|---|---|
| نص السؤال وشرحه ونصوص الخيارات | ✅ | ✅ |
| النوع · الدرجة · الإجابة الصحيحة · عدد الخيارات | ✅ | ❌ |
| حذف السؤال | ✅ | ❌ |
| إضافة سؤال جديد | ✅ | ❌ |
| إعادة الترتيب | ✅ | ✅ (لا يمسّ التصحيح) |

السبب: `exam_answers.question_id` عليه `ON DELETE CASCADE`، فحذف سؤال يمحو
إجابات الطلاب ويترك `exam_attempts.score` يصف امتحانًا لم يعد موجودًا.
والمنع مطبَّق في الخادم لا في الواجهة فقط — اختُبر بتزوير الحقل من المتصفّح.

## قرار D-22 — أزرار ترتيب لا سحب وإفلات
السحب لا يعمل بلوحة المفاتيح، وعلى اللمس يتعارض مع تمرير الصفحة. زرّا
تقديم/تأخير يعملان في الحالتين ويقبلان `aria-label` واضحًا (البند 22).

## قرار D-23 — التبديل في دالة خادم واحدة
`public.move_exam_question` و`public.move_lesson` (هجرة ٢٣): تتحقّق من
صلاحية المدير، تُرقّم الإخوة ترقيمًا متتابعًا يُصلح أي فجوة قديمة، ثم تبدّل
موضعين في عملية واحدة. أمرا UPDATE منفصلان من المتصفّح كانا يتركان ترتيبًا
مكسورًا لو انقطع الاتصال بينهما.

## ثلاثة أخطاء حقيقية ظهرت في الاختبار لا في الأنواع

**١. حذف المرفق كان يترك الملف في التخزين.**
`deleteAttachment` كان يحذف السجل أولًا ثم الملف. لكن سياسة قراءة التخزين
(`attachments_entitled_read`) تتحقّق من وجود صفّ في `attachments` يطابق المسار،
فحذف الصفّ يقطع عن المدير رؤية ملفه ويفشل الحذف **بلا خطأ ظاهر**. كشفه عدّ
`storage.objects` بعد الحذف: السجل صفر والملف باقٍ. الآن التخزين أولًا،
وإن فشل لا يُمسّ السجل.

**٢. نموذج تعديل الدرس كاد يمحو الفيديو.**
استعلام الصفحة كان يجلب `lesson_id` فقط من `lesson_videos`، فكان حقل الرابط
يُفتح فارغًا — و`saveLesson` يحذف الفيديو حين يصله رابط فارغ. صار الاستعلام
يجلب `video_url` ويملأ الحقل.

**٣. حوار التأكيد لا يُفتح بعد إغلاقه بـEsc.**
حدث `close` لا يصعد في الشجرة فلا يلتقطه `onClose` من React، فتبقى الحالة
تظنّ الحوار مفتوحًا. الحلّ لم يكن إضافة مستمع، بل إسقاط الحالة الموازية
أصلًا: الفتح والإغلاق يجريان على العنصر مباشرة (`showModal` / `close`).

## الاختبارات المُجراة
| ما اختُبر | النتيجة |
|---|---|
| تعديل صياغة سؤال في اختبار مقفل + إضافة شرح | ✅ حُفظ وظهر للطالبة في المراجعة |
| تزوير الدرجة من المتصفّح في اختبار مقفل | ❌ رُفض · القاعدة ما زالت ٢٫٠٠ |
| تعديل كامل (النوع → اختيار متعدد، الدرجة ٤، صحيحتان) في اختبار بلا محاولات | ✅ |
| تبديل نوع السؤال لا يمحو الخيارات المكتوبة | ✅ |
| حذف سؤال بالتأكيد | ✅ · والزر غائب في المقفل |
| إعادة ترتيب سؤال في اختبار مقفل | ✅ · النتيجة المسجَّلة ٦٠٪ لم تتغيّر |
| تعديل درس: العنوان والمدة | ✅ · ورابط الفيديو بقي كما هو |
| إعادة ترتيب الدروس ثم إرجاعها | ✅ |
| فكّ ربط مرفق ثم ربطه من جديد | ✅ |
| حذف مرفق | ✅ · السجل والملف اختفيا معًا |
| الحوار: فتح · إغلاق أصلي · إعادة فتح · إلغاء | ✅ |

## حدود معروفة (محدَّثة)
| # | الحدّ | الحالة |
|---|---|---|
| L-14 · L-15 · L-18 · L-21 | — | ✅ أُغلقت في هذه المرحلة |
| L-23 | لا يمكن حذف كورس أو اختبار من اللوحة (الإخفاء فقط) — مقصود حتى الآن | لاحقًا |
| L-24 | تعديل الوحدات أو حذفها غير مبني | لاحقًا |

---

# الواجهة العامة — الشكل المعتمد من المرجع البصري

## ما تغيّر
- **رأس داكن ثابت** بلون `brand-800` مع علامة مرسومة (SVG) وسطر هوية ذهبي،
  وروابط أقسام ذات خط سفلي ذهبي للقسم الحالي، وزرّين: مخطّط وذهبي ممتلئ.
- **واجهة أولى ورقية** (`--color-parchment`) عليها نقش دوائر متشابكة و حروف
  عربية ضخمة باهتة عند الحافة.
- **لوحة الصورة** تملأ الحافة اليسرى وحدّها الأيمن قوس واسع
  (`border-radius: 0 100% 100% 0 / 0 55% 55% 0`) يتتبّعه خط ذهبي رفيع.
- على الشاشات الصغيرة تُستبدل اللوحة بصورة في إطار دائري — القوس الممتدّ
  لا معنى له في عمود واحد.

## قرار D-24 — روابط الرأس تهبط إلى أقسام موجودة
المرجع البصري يحمل خمسة روابط (الرئيسية · المقررات · الاختبارات · الملفات ·
الدعم). بدل أن تشير إلى صفحات مغلقة خلف تسجيل الدخول، بُنيت لها أقسام حقيقية
في الصفحة نفسها تصف ما تفعله المنصة فعلًا. البند 11 يمنع الرابط الذي لا يقود
إلى شيء.

## الشريط الجانبي
صار لوحة عائمة بحواف ناعمة (`inset-y-3 end-3` + `radius-panel` + ظلّ خفيف)
بدل شريط ملتصق بحافة الشاشة، والمحتوى يفسح له `17.5rem`.

---

# المرحلة ١٠ — الحذف: الوحدات والكورسات والاختبارات

## ما نُفِّذ
| الفجوة | ما صار متاحًا |
|---|---|
| L-24 | تعديل اسم الوحدة في مكانه (Enter يحفظ · Esc يلغي) وحذفها |
| L-23 | حذف كورس أو اختبار من قائمته، بشروط |

## قرار D-25 — ما يُحذف هو المحتوى، لا سجلّ الطالب
| العملية | مسموحة متى | لماذا |
|---|---|---|
| حذف وحدة | دائمًا | `lessons.module_id` عليه `SET NULL`: الدروس تبقى كاملة وتصير «بلا وحدة» |
| حذف كورس | لا اشتراكات **ولا أكواد** | `enrollments` و`activation_codes` عليهما `RESTRICT` — القاعدة نفسها تمنعه |
| حذف اختبار | لا محاولات | `exam_attempts` عليه `CASCADE`: الحذف يمحو نتائج الطلاب |

الفحص يسبق الحذف لا ليحمي — القاعدة تحمي — بل ليقول السبب بالعربية:
«لا يمكن حذف كورس اشترك فيه ١ طالب… أرشفي الكورس بدل حذفه»
بدل خطأ مفتاح أجنبي خام لا يفهمه أحد.

## قرار D-26 — زرّ الحذف خارج رابط الصفّ
صفوف الكورسات والاختبارات روابط تغطّي الصفّ كله. زرٌّ داخل `<a>` تركيبٌ
غير صالح ويسرق النقرة من الرابط، فصار الصفّ `flex`: الرابط يملأ ما عدا
طرفه، وزرّ الحذف بجانبه خارجه.

## الاختبارات المُجراة
| ما اختُبر | النتيجة |
|---|---|
| تعديل اسم وحدة | ✅ |
| حذف وحدة فيها درس | ✅ · الوحدة اختفت والدروس الثلاثة باقية |
| حذف كورس فيه طالب | ❌ رُفض برسالة واضحة · الكورس باقٍ |
| حذف اختبار بدأه طالب | ❌ رُفض · الاختبار باقٍ |
| حذف كورس مسوّدة بلا اشتراكات | ✅ · سقط معه درسه ووحدته واختباره |
| حذف اختبار بلا محاولات | ✅ |
| بعد كل ذلك: الاشتراكات والمحاولات | ٢ و١ — لم يُمسّ سجلّ طالب واحد |

## حدود معروفة (محدَّثة)
| # | الحدّ | الحالة |
|---|---|---|
| L-23 · L-24 | — | ✅ أُغلقت |
| L-25 | حذف كورس له أكواد يتطلّب حذف الأكواد أولًا (لا يوجد حذف أكواد بعد) | لاحقًا |

---

# الواجهة العامة — إعادة بناء دقيقة على المرجع البصري

## الهوية اللونية المعتمدة
| الرمز | القيمة | الاستخدام |
|---|---|---|
| `brand-800` | `#0B4038` | الشريط العلوي · العناوين · الزرّ الأساسي |
| `brand-700` | `#124C43` | الأزرار داخل اللوحة · العنصر النشط |
| `accent` | `#C7A04A` | الخطوط والتعبئة والقوس الذهبي |
| `accent-ink` | `#8A6A1C` | **النص** الذهبي على خلفية فاتحة |
| `accent-soft` | `#E3C878` | الذهبي الفاتح على الأخضر الداكن |
| `canvas` | `#FAFAF7` | خلفية الواجهة الأولى |
| `ink` / `ink-muted` | `#123D37` / `#53615E` | النص الأساسي والثانوي |
| `ornament` | `#E8E8E2` | الزخارف وحدها — لا يُكتب به نص |

## قرار D-27 — ذهبيّان لا ذهبي واحد
`#C7A04A` على الأبيض المائل يعطي تباينًا **٢٫٧٩:١**، والأبيض فوقه **٢٫٩١:١** —
كلاهما يرسب في WCAG AA. اللون باقٍ كما هو للخطوط والقوس والتعبئة، لكن:
- النص الذهبي يستعمل `accent-ink` → **٤٫٨٣:١** ✓
- نصّ الزرّ الذهبي يستعمل `brand-900` → **٦٫٢٦:١** ✓

القياس جرى على الصفحة المعروضة لا على الورق.

## قرار D-28 — الرابط النشط يتبع القسم المعروض
الخط الذهبي تحت «الرئيسية» كان ثابتًا. صار `IntersectionObserver` يحدّد
القسم الظاهر فعلًا، فلا يدّعي الشريط موضعًا يخالف ما يقرأه الزائر.

## بنية المكوّنات
```
src/lib/site/content.ts        كل النصوص العربية — تُعدَّل من مكان واحد
src/components/site/logo.tsx             LogoMark + Logo
src/components/site/navigation-links.tsx الروابط + تتبّع القسم النشط
src/components/site/cta-buttons.tsx      AuthButtons + HeroButtons
src/components/site/navbar.tsx           الشريط + قائمة الموبايل
src/components/site/hero-section.tsx     الواجهة الأولى + الزخارف + القوس
```

## الاستجابة
| المقاس | السلوك |
|---|---|
| Desktop ≥1024 | التكوين المرجعي: نص يمينًا، صورة يسارًا داخل قوس ذهبي |
| Tablet 768 | عمود واحد: النص أولًا ثم الصورة في إطار دائري · القائمة زرّ |
| Mobile 375 | كالسابق، والزرّان بعرض كامل داخل القائمة المنسدلة |

القائمة تُغلق بـEsc وبالنقر خارجها وبالنقر على رابط، و`aria-expanded`
و`aria-controls` مضبوطان. لا تجاوز أفقي في أي مقاس (قيس: `scrollWidth === clientWidth`).

## ما خالفتُ فيه المرجع عمدًا
| المرجع | ما نُفِّذ | السبب |
|---|---|---|
| «منصتي التعليمية» | اسم الأستاذة الحقيقي | منتج حقيقي لا قالب — والاسم ثابت واحد في `content.ts` |
| «مشروعك بتقريب» | «مشروحة بترتيب» | الأولى قراءة OCR للصورة وليست عربية سليمة |
| صورة طالبة | صورة الأستاذة | لا تُولَّد صور أشخاص لمنتج حقيقي |
| نص أبيض على الذهبي | أخضر شبه أسود | الأبيض يرسب في تباين AA |
